# Cloudflare Tunnel token (remotely-managed tunnel).
# Get it from Zero Trust Dashboard → Networks → Tunnels → (your tunnel) → Install connector.
TUNNEL_TOKEN=PASTE_YOUR_TUNNEL_TOKEN_HERE

# Uncomment to enable Cloudflare Tunnel every time
# COMPOSE_PROFILES=tunnel 

# HLS tuning
HLS_TIME=5
HLS_LIST_SIZE=6
OFFLINE_CHUNK_SECONDS=3

# ffmpeg tuning
OFFLINE_VIDEO_FPS=1
OFFLINE_AUDIO_BITRATE=64k
CLEAN_TS=1

# Stream keys (OBS stream keys)
STREAM_KEY_NGINX0=nginx0
STREAM_KEY_NGINX1=nginx1

# Optional: enable second stream by running:
#   docker compose --profile nginx1 up -d
